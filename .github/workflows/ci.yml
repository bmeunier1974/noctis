name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref to save minutes.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: quality (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 3.11 is the floor (requires-python, ruff/mypy targets); 3.12 proves forward compat.
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}

      # `--locked` is the point: fail if pyproject.toml changed without regenerating uv.lock.
      # Installs core + the default dev group only — no heavy extras, no API key. The suite
      # is designed to pass on that install (tests/conftest.py isolates env vars).
      - name: Sync (locked, core + dev)
        run: uv sync --locked

      - name: Tests
        run: uv run --no-sync pytest

      - name: Lint (ruff check)
        run: uv run --no-sync ruff check .

      - name: Format check (ruff format)
        run: uv run --no-sync ruff format --check .

      - name: Type check (mypy)
        run: uv run --no-sync mypy

      # The behavioural ratchet: recompute the per-component engine fingerprint and compare it
      # with the committed engine_fingerprint.json. Arbiter drift (what passes, what a number
      # means) without an ENGINE_VERSION bump FAILS here; searcher-tier drift warns in the job
      # log and passes. Exit code and message come from noctis.observability.engine_ratchet.
      - name: Engine fingerprint ratchet
        run: uv run --no-sync python scripts/engine_fingerprint.py --check

      - name: Build package
        run: uv build
